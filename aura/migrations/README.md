# Database migrations

SQL changes for the Aura Supabase (Postgres) database, kept here as a **historical
record**. There is **no migration runner** — each file was applied manually via the
**Supabase SQL Editor**, in roughly the chronological order below.

> Treat this folder as the source of truth for "what shaped the schema and when". The
> human-readable schema overview lives in [`../docs/DATABASE.md`](../docs/DATABASE.md).

## Conventions

- Most columns are **camelCase** (quoted identifiers, e.g. `"propertyId"`). A few legacy
  F&B/breakfast tables (`fb_*`) use **snake_case** (`property_id`, `ala_carte`) — match the
  table you are querying.
- Tables are scoped per property for multi-tenancy; RLS policies live in
  `rls_all_properties.sql` and the `*_rls.sql` files.
- New changes: add a new `.sql` file here (don't edit applied ones), append it to the table
  below with its date and a one-line description, and apply it with `pnpm db:sql` — **DEV
  first, production after it works**:

  ```bash
  pnpm db:sql migrations/nova_coisa.sql                # DEV (padrão)
  pnpm db:sql migrations/nova_coisa.sql --target prod  # produção, com confirmação
  ```

  O script roda tudo em uma transação (um erro no meio desfaz tudo), ao contrário do SQL
  Editor. Detalhes em [`../docs/DEV-DATABASE.md`](../docs/DEV-DATABASE.md).

## Index (chronological)

| Date | File | What it does |
|------|------|--------------|
| 2026-03-04 | `enable-realtime.sql` | Enable Supabase Realtime on core tables |
| 2026-03-06 | `add_maintenance_columns.sql` | Extra columns on maintenance tables |
| 2026-03-06 | `setup_surveys.sql` | Surveys schema |
| 2026-03-06 | `setup_surveys_rls.sql` | RLS policies for surveys |
| 2026-03-11 | `add_concierge_tables.sql` | Concierge requests + catalog tables |
| 2026-03-11 | `add_dnd_maintenance_and_system_bugs.sql` | DND, maintenance flags, system bug log |
| 2026-03-11 | `add_raca_column.sql` | `guests.raca` — raça/etnia do hóspede para o FNRH (não tem relação com pets) |
| 2026-03-11 | `alpha_bug_fixes.sql` | Assorted alpha-phase fixes |
| 2026-03-11 | `fb_migration.sql` | Food & Beverage base schema |
| 2026-03-11 | `fb_orders_table.sql` | F&B orders table |
| 2026-03-11 | `fb_advanced_update.sql` | F&B advanced fields |
| 2026-03-18 | `fix_structure_bookings_rls.sql` | Fix RLS for structure bookings |
| 2026-03-18 | `rls_all_properties.sql` | Per-property RLS (multi-tenant baseline) |
| 2026-03-19 | `fix_structure_bookings_rls_v2.sql` | Structure bookings RLS, revision 2 |
| 2026-03-19 | `fix_survey_rls.sql` | Fix survey RLS |
| 2026-04-23 | `normalize-names-phones.sql` | One-off data fix: normalize guest names/phones |
| 2026-06-03 | `add_cabin_ignore_occupancy.sql` | Cabin flag: ignore occupancy |
| 2026-06-03 | `add_internal_stays.sql` | Support internal (staff) stays |
| 2026-06-05 | `add_resort_map.sql` | Resort map schema |
| 2026-06-05 | `add_resort_map_cabins.sql` | Resort map cabin positions |
| 2026-06-06 | `add_daily_release.sql` | Daily release feature |
| 2026-06-06 | `add_structure_translations.sql` | i18n columns for structures |
| 2026-06-07 | `add_map_pois.sql` | Map points of interest (POIs) |
| 2026-06-11 | `stock_phase0.sql` | Stock/inventory: shared categories (consumable + asset) |
| 2026-06-11 | `stock_phase1.sql` | Stock module — phase 1 |
| 2026-06-11 | `stock_phase1_assets_media.sql` | Stock phase 1: asset media |
| 2026-06-11 | `stock_phase1_purchase_invoice.sql` | Stock phase 1: purchase invoices |
| 2026-06-15 | `stock_phase2.sql` | Stock module — phase 2 |
| 2026-06-16 | `stock_phase3.sql` | Stock module — phase 3 |
| 2026-06-16 | `stock_phase3b_drop_minibar.sql` | Stock phase 3b: drop minibar |
| 2026-06-18 | `add_area_reviews_moderation.sql` | Area reviews moderation |
| 2026-06-18 | `add_survey_curated.sql` | Curated survey (Survey 2.0) |
| 2026-06-19 | `add_breakfast_venue.sql` | Breakfast venue / salão do café |
| 2026-06-19 | `add_poi_instagram.sql` | Instagram field on POIs |
| 2026-06-19 | `stock_phase4.sql` | Stock module — phase 4 |
| 2026-08-01 | `stock_movement_targets.sql` | Movimentações: destino "colaborador" (`fromStaffId`/`toStaffId`) |
| 2026-08-01 | `stock_phase6_responsible.sql` | Movimentações: responsável pela ação + `batchRef` (lote) |
| 2026-08-01 | `stock_phase6_cabin_locations.sql` | Índices para cabanas como locais derivados (só índices, sem UPDATE) |
| 2026-08-01 | `add_housekeeping_skip_columns.sql` | `skippedAt` + `guestName` em `housekeeping_tasks` (faxina não realizada) |
| 2026-08-02 | `patrimonio_phase1.sql` | Patrimônio: `publicCode` (plaqueta QR, imutável), baixa/alienação, `asset_movements`, nº sequencial e `maintenance_tasks."assetId"` |
| 2026-08-02 | `patrimonio_phase3_inventario.sql` | Patrimônio: conferência física (`asset_inventory_counts` / `asset_inventory_items`) |
| 2026-08-03 | `add_cabin_conference_author.sql` | `cabinCheckedBy` + `cabinCheckedAt` em `housekeeping_tasks` (autor da conferência de saída) |
| 2026-08-04 | `tarifario_phase1.sql` | Tarifário: tabelas de preço (`rate_tables`), regras de calendário (`rate_periods`) e config comercial (`rate_settings`) — port do SIT |
| 2026-08-04 | `tarifario_phase2_orcamentos.sql` | Tarifário fase 2: orçamentos salvos + funil de vendas (`rate_quotes`) com cliente/lead vinculável a hóspede e estadia |
| 2026-08-04 | `cabin_categories.sql` | Categoria de cabana vira entidade (`cabin_categories`): `cabins."categoryId"`, `rate_tables.prices` reindexado por id, consolidação das grafias divergentes e migração dos links do site |
| 2026-08-04 | `financeiro_phase1_diarias.sql` | Financeiro fase 1: fólio vira extrato (`folio_items.type` debit/credit + `refDate` p/ diárias idempotentes) e estadia ganha `nightlyRate`/`lodgingTotal`/`rateQuoteId` |
| 2026-08-04 | `financeiro_phase2_diaria_editavel.sql` | Financeiro fase 2: `stays."lodgingPaused"` (pausa o cron) e `stays."nightlyOverrides"` (valor por noite; 0 = noite não cobrada) |
| 2026-08-04 | `weddings_status_lost.sql` | Casamentos: status `lost` (negociação perdida ≠ cancelado) — recria a CHECK constraint de `status` e adiciona `"lostReason"`/`"lostAt"` |
| 2026-08-05 | `weddings_lead_validity.sql` | Casamentos: validade do lead — `"followUpAt"`/`"expiresAt"` + índices parciais; padrões por propriedade em `properties.settings.weddingLead` |
| 2026-08-08 | `crm_phase1_foundation.sql` | CRM fase 1: origem (`source`) + prazos (`followUpAt`/`expiresAt`/`sentAt`/`lostAt`) em `rate_quotes`, contato do casal em `weddings`, e histórico compartilhado `crm_interactions` |
| 2026-08-08 | `crm_phase1_backfill.sql` | CRM fase 1 (backfill): snapshot antigo ganha `categoryId`, `selectedCategory` nome→id, prazos retroativos e `categoryLinks`→`siteUrl` — com sondas antes de cada UPDATE |
| 2026-08-08 | `crm_phase2_negotiated_value.sql` | CRM fase B.5 (1/4): `rate_quotes."negotiatedValue"` (valor fechado na conversa, vence a tabela) e CHECK de `crm_interactions.kind` recriado com `value_change`/`guest_linked`/`alarm_done` |
| 2026-08-08 | `crm_phase2_alarms.sql` | CRM fase B.5 (2/4): `crm_alarms` (follow-up/cobrança/lembrete para leads E fechados, `entityLabel` snapshot, índice parcial de abertos, RLS + realtime p/ badge) |
| 2026-08-08 | `weddings_installments.sql` | CRM fase B.5 (3/4): `wedding_installments` (parcelas reais com vencimento; FK via `DO` dinâmico pois a PK de weddings não é versionada) + backfill das 2 legadas e 3ª derivada num único INSERT idempotente |
| 2026-08-08 | `crm_phase2_waitlist.sql` | CRM fase B.5 (4/4): `waitlist_entries` (lista de espera para períodos — nome/telefone/período, status waiting→contacted→converted/archived, `quoteId` de rastro) |
| 2026-08-09 | `crm_phase3_quote_rooms.sql` | CRM fase 3: `rate_quotes."rooms"` (acomodações pedidas na MESMA negociação, com as opções de cada uma) + `"acceptedAt"` (aceite na proposta pública) + kind `client_accepted` |
| 2026-08-10 | `tarifario_inclusions.sql` | `rate_settings."inclusionsText"` — "O que está incluso" (uma linha por item) exibido na proposta pública acima das regras; editável em Tarifário → Comercial |
| 2026-08-10 | `tarifario_phase4_flutuacoes_arquivo.sql` | Tarifário fase 4: `rate_fluctuations` (preset de flutuação atribuído a um período — cotação "Automática" aplica noite a noite), `rate_table_versions` (histórico de preços — snapshot antes de cada alteração/exclusão), `rate_tables."archivedAt"/"archivedBy"` (arquivo) e `rate_quotes."fluctuationAuto"` |
| 2026-08-17 | `stays_multi_pet.sql` | Pets: `stays."pets"` (array jsonb) com backfill de `"petDetails"` — mais de um pet por estadia; `hasPet` e `petDetails` (= pets[0]) seguem mantidos |
| 2026-08-17 | `crm_phase4_intl_orcamento.sql` | CRM fase 4: `rate_quotes."clientDocumentType"`/`"clientLanguage"` (documento internacional + idioma do hóspede) e `rate_settings.*_en`/`*_es` (templates de WhatsApp + "o que está incluso" em 3 idiomas) |
| 2026-08-18 | `weddings_guest_site.sql` | Site dos noivos: códigos de 6 dígitos (`"guestCode"`/`"coupleCode"`), tabela vinculada (`"rateTableId"`), janela de extensão (`"maxExtendNights"`), `"siteEnabled"`/`"siteConfig"` e índice parcial de pré-reservas abertas em `rate_quotes` |
| 2026-08-20 | `stock_location_policy.sql` | Estoque Etapa A: `stock_locations."policy"` (stock/consume_all/consume_categories, ponto de consumo) + `"consumeCategoryIds"` e `stock_products."neverConsume"` (bem durável isento da conversão transferência→saída) |
| 2026-08-20 | `restock_requests.sql` | Reposição Etapa B: tabela `restock_requests` (pedido camareira→mensageiro fora do Concierge, com fontes planejada/fallback/usada) + `stock_products."maidRequestable"/"deductMode"/"deductLocationId"` e `stock_categories."deductLocationId"` (cadeia de baixa produto→categoria) |
| 2026-08-24 | `crm_intake_proposta.sql` | Proposta pública: `rate_quotes."intake"`/`"intakeAt"` (cadastro do titular preenchido pelo CLIENTE depois do aceite — titular, endereço, acompanhantes, placa, pet, pagamento e consentimento; `intakeAt` é a trava do link), `rate_quotes."clientInstagram"` (lead sem telefone/e-mail), `rate_settings."paymentOptions"` (condições de pagamento em 3 idiomas) e kind `client_intake` |
| 2026-08-24 | `add_stays_view_prefs.sql` | Estadias fase 1: `staff."staysViewAtivas"`/`"staysViewFuturas"` (modo cartão/compacto/lista por aba, por usuário — o PC da recepção é compartilhado, então localStorage não serve) |
| 2026-08-24 | `add_stay_account_state.sql` | Estadias fase 2: `stays."keyStatus"/"keyStatusAt"/"keyStatusBy"`, `"loanedItemsStatus"` e `"lostItemsResolution"/"lostItemsResolvedAt"/"lostItemsResolvedBy"` — chave, empréstimo e objeto esquecido viram estado da conta (com backfill honesto do que o banco já sabia) |
| 2026-08-24 | `backfill_bill_closed_at.sql` | Estadias fase 2: fecha `billClosedAt` das `finished` sem pendência (251 na aplicação) — sem isso todo o histórico voltaria para a aba Ativas, já que a conta aberta passou a ser o que segura a estadia lá |
| 2026-08-24 | `add_cabin_active.sql` | Estadias fase 3: `cabins.active` (fora de operação) — separa "desativada" de `ignoreInOccupancy` ("não conta na ocupação"); a grade de últimas saídas só mostra cabana ativa |
| 2026-08-24 | `unarchive_stays.sql` | Estadias fase 3: devolve as `archived` ao histórico como `finished` com `billClosedAt` (8 na aplicação) — o botão "Arquivar" saiu e o que ele escondeu precisava voltar |
| 2026-08-24 | `hsystem_integration.sql` | Hsystem (HUNIT) fase 1: `stays."source"`/`"externalId"`/`"externalRoomId"` com índice único parcial (a mesma reserva do canal não entra duas vezes) + `hsystem_reservations` (espelho cru da reserva, com `contentHash` para detectar mudança) e `hsystem_sync_log` |
| 2026-08-25 | `guests_created_at.sql` | `guests."createdAt"`: a coluna entra **sem** default no ADD (legado não é carimbado com a data da migration) e só depois ganha `DEFAULT now()` para linhas novas; backfill pelo primeiro audit de `CREATE` — em produção, 200 fichas recuperaram a data real e 192 antigas ficam NULL, que é a verdade |
| 2026-08-25 | `stock_purchase_xml_import.sql` | Compras pelo XML da NF-e: identidade fiscal da nota em `purchases` (`invoiceKey` com índice único parcial por propriedade — a mesma nota não entra duas vezes —, `invoiceSeries`/`invoiceModel`/`invoiceXmlUrl`/`invoiceDeclaredTotal`/`importSource`) e `supplier_product_map`, o de-para que se lembra: fornecedor + `cProd` → produto daqui, com fator de embalagem (1 CX = 12 un) e as marcas de linha de patrimônio / linha ignorada |
| 2026-08-26 | `messages_hot_indexes.sql` | Dieta de egress: três índices PARCIAIS em `messages` (36 mil linhas, só pkey + `propertyId` — que não filtra nada porque quase toda mensagem é da mesma propriedade). `idx_messages_unread` (contador do sino), `idx_messages_queue` (fila do cron `process-messages`) e `idx_messages_api_id` (webhook casando o id da Evolution). As três varriam a tabela inteira — ~1 486 blocos por chamada; com índice o sino vira *Index Only Scan* com `Heap Fetches: 0` em **4 blocos**. Somam 72 kB. Aplicar com `--no-atomic` (CONCURRENTLY não roda em transação) |
| 2026-08-26 | `events_type_hygiene.sql` | Eventos fatia 1 (higiene): backfill de `events.type` — 8 das 13 linhas de produção tinham `type='internal'`, valor que nunca existiu no union TypeScript (`local`/`external`/`private`), o que deixava todo mapa de rótulo/tom devolvendo `undefined` e tornaria o filtro positivo das leituras públicas cego para esses eventos. Vira `local` (decisão do dono: Sunset Maram é evento nosso) e ganha CHECK para não voltar. CHECK de `category` fica de fora de propósito — a lista vai ser revista com o parceiro |
| 2026-08-26 | `rls_close_public_policies.sql` | **Fecha vazamento vivo.** A chave pública (`anon`, que viaja no JS do site) lia **e escrevia** em `messages` (36 246 linhas — telefone do hóspede em `to`, conteúdo em `body`), `communications` (409) e `breakfast_visitors` (4): três policies `FOR ALL TO PUBLIC USING (true)` que sobreviveram à remediação da chave pública (aquela rodada revogou o GRANT de 19 tabelas; nestas o grant ficou e a policy PUBLIC o encontrou). Não era só leitura — `anon` tinha INSERT/UPDATE/DELETE/TRUNCATE, ou seja, dava para enfileirar mensagem que o cron mandaria pelo WhatsApp da pousada e para apagar o histórico. Cria `property_scoped_all` em `communications` (única sem escopo), dropa as três policies públicas e REVOKE do `anon`. Verificado com a própria chave: antes 206 + contagem, depois 401 |
| 2026-08-26 | `rls_drop_public_true_policies.sql` | Fatia 3 do Eventos + as 15 tabelas que podiam vir junto. `events` ganha a `property_scoped_all` que FUNCIONA (a antiga comparava com `auth.jwt() ->> 'propertyId'`, claim que o JWT não tem — nunca casava, então o `USING(true)` era o único acesso e dropar sozinho derrubaria o realtime da tela). Depois caem as 16 policies `USING(true)` das 15 tabelas que já tinham rede escopada por trás. **Não era teórico:** simulando a sessão do admin de `estanciadovale`, ele enxergava 394 hóspedes, 448 estadias, 192 fólios e 1 807 tarefas da Fazenda do Rosa; depois, zero. Seguro porque só super_admin troca de propriedade, zero staff ativo sem `propertyId`, e a escrita em `staff` já vai por service-role. As ~40 tabelas SEM rede (estoque, tarifário, compras, CRM) ficaram de fora — nelas dropar corta o acesso |
| 2026-08-26 | `guarita_phase1.sql` | Guarita/Estacionamento fase 1: `vehicles` (placa normalizada com índice único por propriedade, dono, tipo e marcação de placa problemática), `parking_rates` (a tarifa é POR DIA — R$ 150 em dia bonito, R$ 30 na baixa, e dia que nem abre), `parking_shifts` (índice parcial garantindo UM turno aberto por vez) e `vehicle_movements` (entrada/saída, valor, forma, bandeira e NSU). As quatro nascem com RLS ligado e REVOKE de `anon`/`authenticated` — o acesso é só por service-role via `/api/field/guarita`. Mais `staff."vehiclePlate"`, que faz o carro da equipe se identificar sozinho na guarita |
| 2026-08-28 | `guarita_enable_flag.sql` | Liga `settings.hasGuarita` em `fazenda-do-rosa` e `fazenda-modelo-aura` (merge raso no jsonb). A flag nasceu com default DESLIGADO, então precisa rodar **antes ou junto** do deploy que leva o gate — senão a Guarita some do menu no mesmo deploy e parece que `guarita_phase1` não pegou. `estanciadovale` fica de fora de propósito: uma pousada de uma cabana sem portaria é o caso que justifica a flag existir |
| 2026-08-28 | `perf_indices_postgrest_parametros.sql` | **Conserta a dieta de egress de 26/08, que nunca pegou.** Os três índices de `messages_hot_indexes.sql` são PARCIAIS com literal no predicado (`WHERE direction='inbound' AND "isReadByAdmin"=false`, `WHERE status='queued'`). O PostgREST manda todo valor como parâmetro (`$1`, `$2`), o Postgres monta um **plano genérico** e não consegue provar que `$2 = 'inbound'` — então nunca alcança o índice. Provado em produção: `idx_messages_queue` com `idx_scan = 0`, nunca usado uma única vez; `messages` com 183 316 seq scans e **3,3 bilhões de linhas lidas** numa tabela de 37 mil. Repare que `idx_messages_api_id` FUNCIONA: parcial por `IS NOT NULL` não depende de parâmetro, então o planejador prova. Troca pelos compostos comuns — `idx_messages_inbox` (sino + lista de não lidas), `idx_messages_status_scheduled` (fila do `process-messages`), `idx_messages_status_updated` (watchdog), mais `housekeeping_tasks(propertyId,status)` (o mesmo caminho do 504 de `/api/field/housekeeping-tasks`) e `structure_bookings(propertyId,status,type,createdAt)`. A/B em produção na mesma sessão, contador real do sino: antes *Index Scan* descartando 31 303 linhas em 1 386 blocos, **35,3 ms**; depois *Index Only Scan* em 668 blocos, **1,38 ms** — 25×, numa query que roda ~96 mil vezes. Os dois índices mortos caem no fim; rollback escrito no rodapé do arquivo |
| 2026-08-28 | `platform_panel_rpcs.sql` | Seis funções de agregação para o painel de plataforma (`/admin/core/dashboard`): série diária de trabalho, adoção de módulo por propriedade, saúde/qualidade de dado, plantão (fila, crons, logins recusados), carga do banco (`pg_stat_statements` classificado) e acervo do Storage. Existem porque **o PostgREST não faz GROUP BY**: sem elas o painel baixaria ~18 mil timestamps de `messages` por carga (meio megabyte de egress para desenhar 30 pontos) ou dispararia ~200 requisições `head:true`. Com elas a página inteira custa **≈15 KB** de resposta. Todas `SECURITY DEFINER` (precisam de `extensions.pg_stat_statements` e `storage.objects`) e por isso todas com `REVOKE ALL FROM public, anon, authenticated` + `GRANT` só a `service_role` — conferido: `has_function_privilege('anon', …)` devolve `false`. Cuidado de esquema embutido: `fb_orders` é o legado snake_case (`created_at`/`property_id`), então tabela e coluna viajam juntas na lista de VALUES. Somente leitura — rollback é `DROP FUNCTION` |
| 2026-08-28 | `push_subscriptions.sql` | Cria a tabela que **13 pontos do código sempre esperaram e nunca existiu** (`src/lib/push-notify.ts`, `/api/push/subscribe`, `/api/push/unsubscribe`, `/api/push/send/*`): o Web Push nunca chegou a ninguém em produção e falhava calado, porque o `error` do Supabase era descartado no caminho de envio e um `data: null` é indistinguível de "ninguém inscrito". Formato tirado do que o código já usa — único em `endpoint` (o upsert do subscribe depende dele), índices `(propertyId, staffId)` e `(propertyId, role)`, RLS + REVOKE de `anon`/`authenticated` (a linha guarda a credencial de mandar notificação para o aparelho de um funcionário) |
| 2026-08-28 | `phone_ddi_backfill.sql` | Telefone brasileiro guardado sem o DDI 55 — 53 contatos, 15 hóspedes e 1 funcionário em produção. Custou 22 mensagens mortas como "Bad Request" entre março e agosto, uma de cada vez. Só toca 10/11 dígitos (DDD + 8/9); deixa intactos os internacionais reais (54/595/49), os JIDs de grupo do WhatsApp (18 dígitos, começam com 120363) e o dado corrompido de 14–22 dígitos, que prefixar só disfarçaria. A regra do sistema continua sendo o DDI explícito no cadastro — **não** existe auto-55 no servidor (ver `src/lib/phone.ts`) |
| 2026-08-29 | `changelog_0_10_a_0_11.sql` | Publica as releases **0.10.0** (Estadias e Conta), **0.10.1** (A nota entra pelo XML, segurança e peso das telas) e **0.11.0** (Guarita e o painel da plataforma) — 48 itens. O changelog vive no banco (`changelogs` + `changelog_entries`) e alimenta `/admin/changelog` e a home pública; o último publicado era o 0.9.0, de 22/08, sete dias e ~60 commits atrás. Idempotente por versão: a release só entra se não existir, e os itens só se aquela release ainda não tiver nenhum |
| 2026-08-30 | `add_unit_status.sql` | `structures."unitStatus"` (jsonb): estado operacional **por unidade** de uma estrutura. Existe porque a trava diária (`requiresDailyRelease`/`releasedForDate`) vale para a estrutura inteira — com uma das duas jacuzzis quebrada, a recepção não tinha onde registrar isso e passou a lançar **5 `maintenance_block` por dia**, um por slot (medido em produção: Unidade Branca desde 26/08, Azul no começo do mês). O novo estado é PERSISTENTE: marca uma vez, some da agenda e do portal até alguém devolver — não reseta à meia-noite, ao contrário de `releasedForDate`. Formato `{ "<unitId>": { status, note, since, byName } }`, unidade **ausente = disponível** (só a que saiu ganha chave). Decisão do dono: a liberação diária CONTINUA por estrutura — o custo real era a rotina de rebloquear, não a granularidade do botão. **Aplicar antes do deploy do código**: `/api/guest/structure-bookings` passou a pedir `unitStatus` no `select`, e sem a coluna o PostgREST erra, a leitura vira `null` e a trava de liberação diária deixa de valer em silêncio |
| 2026-09-01 | `timeclock_phase1.sql` | Módulo **Ponto**, fase 1: `staff."timeSource"` (`none`·`aura`·`rep`, padrão `none`) e a tabela `time_clock_events`. Os três estados são **exclusivos** por decisão do dono — ou a pessoa bate no Aura, ou bate no relógio, ou não bate — o que impede a mesma pessoa acumular duas contagens de horas concorrentes. A tabela guarda **batidas**, não jornadas: nada de `entrada1/saida1/entrada2/saida2`, que quebraria no primeiro dia com dois intervalos (a folha de agosto do prestador tem um dia com três). O par entrada→saída, o total e a pendência são derivados em `src/lib/timeclock.ts`, no cliente — o servidor roda em UTC e não pode decidir onde o dia da pousada começa. `source` fica na BATIDA e não só no cadastro, então trocar alguém de `rep` para `aura` não reescreve o passado; ajuste preserva `originalTs` e exclusão é lógica com motivo. `(repSerial, nsr)` único prepara o import do AFD do REP Hexa (fase 2), onde reimportar o mesmo arquivo é o caso normal. Índices NÃO parciais de propósito — o predicado que o PostgREST não prova é o mesmo buraco de `perf_indices_postgrest_parametros.sql`. RLS + REVOKE de `anon`/`authenticated`: tudo passa por `/api/admin/timeclock` |
| 2026-09-03 | `hr_fatia0_rls_escalas.sql` | **RH v2, fatia 0 — fecha o vazamento medido em 03/09** (`docs/HR-V2.md` §1.7). `staff_schedules` e `staff_schedule_overrides` tinham cada uma uma política `SELECT` com `auth.role() = 'authenticated'` — sem filtro de propriedade e sem filtro de pessoa: **qualquer usuário logado lia o horário e a folga de qualquer funcionário de qualquer propriedade**. Apagadas em vez de escopadas por `propertyId` porque ninguém lê essas tabelas pelo client do browser — os dois leitores são rotas que usam `supabaseAdmin` (service role, que ignora RLS) e já filtram à mão (`api/admin/staff/schedule-overrides/route.ts:36`). Política escopada seria código morto com aparência de proteção. O estado final é o mesmo que `time_clock_events` e `staff_schedule_checkpoints` já tinham e que está **correto**: RLS ligada, zero política, nega tudo fora do service role — não "consertar" essas duas depois achando que falta política. As tabelas morrem na fatia 1 (decisão 10 — zerar); este arquivo existe porque a fatia 1 não é hoje e o vazamento era hoje. Reversível recriando a política — mas nunca na versão `authenticated` |
| 2026-09-03 | `hr_fatia1_modelo.sql` | **RH v2, fatia 1 — o modelo novo de escala** (`docs/HR-V2.md`). Cria `work_pattern_templates`, `staff_work_patterns`, `staff_absences`, `staff_shifts` e `schedule_periods`, migra os 19 `staff.scheduleConfig` e **zera** o modelo velho. Três decisões que o arquivo carrega no comentário e que não devem ser reinventadas: (1) **não existe coluna `scheduleType`** — a leitura de `schedule-calculator.ts` mostra que os três "tipos" não são a mesma coisa (`6x1` é regra semanal + uma regra periódica de domingo, `5x2` é regra semanal, só `12x36` é ciclo de verdade), e tratá-los como um campo foi o que obrigou o `sundayOffCycle` a existir como booleano pendurado e o `Domingo Mes` a virar texto livre num campo de motivo; agora são duas BASES (`weekly`/`cycle`) mais uma lista de REGRAS em jsonb. (2) **o `history` não é migrado** — ele PARECE versionamento e é log de save: a Grazi tem 18 itens com 5 configurações distintas e sete deles idênticos ao atual, com `endDate` fora de ordem e repetidas, o que já torna arbitrária a escala que o sistema mostra para o passado; migra-se só o config atual, vigente a partir de 01/09/2026, e o blob cru fica arquivado em `legacyConfig`. (3) **as tabelas velhas ficam de pé, só o conteúdo sai** — derrubá-las agora quebraria os 11 call sites que ainda leem as rotas antigas; a casca cai depois do deploy. Traz também os dois modelos de jornada que a medição justifica (6x1 08:20–16:20 com 9 pessoas, 12x36 08:30–20:30 com 4) e a flag `hasRH` com **backfill explícito** (regra 6 da modularização): ligada na Fazenda, desligada nas outras. Todas as tabelas nascem com RLS ligada e **zero política** — nega tudo fora do service role, igual a `time_clock_events`; não "consertar" isso depois. Conferido no DEV: o motor novo (`src/lib/schedule-engine.ts`) reproduz o calculador antigo com **zero divergências em 6.935 dias-pessoa** (19 pessoas × 365 dias) |
| 2026-09-03 | `hr_fatia4_afd.sql` | **RH v2, fatia 4 — o import do AFD.** Cria `afd_imports` (o arquivo lido, com o hash que impede reimportar o mesmo duas vezes) e `staff_documents`, onde mora o identificador que o REP usa e que não podia ficar em `staff` (RLS deny-all: é dado de documento). Ver `docs/HR-V2.md` e a memória do import do Hexa — o Secullum **Offline** não exporta AFD, só o Web, e são **três** leiautes (1510/595/671), não dois |
| 2026-09-03 | `pet_exception_phase1.sql` | **Política Pet, fase 1 — o pedido de exceção e o aceite gravado** (`docs/PET-POLICY.md`). `stays."petException"` (jsonb com `status`/`reasons`/`requestedAt`/`decidedAt`/`decidedBy`/`authorizedBy`/`note`) e `stays."petPolicyAcceptedAt"`. O aceite existe porque o checkbox da política pet **nunca foi gravado** — só destravava o botão — e sem registro não há base para recusar entrada nem para cobrar. Do hóspede só entra `status='pending'`: o servidor rebaixa qualquer status forjado (`_sanitizePetException`) e uma exceção já decidida não volta a pendente num reenvio do formulário. Índice **parcial por `IS NOT NULL`**, e não por igualdade a literal — provado alcançável com `force_generic_plan` + `PREPARE` (`Index Cond: (("petException" ->> 'status') = $1)`), sem repetir a armadilha de `perf_indices_postgrest_parametros.sql`. Backfill **explícito** das chaves de `settings` (`acceptsPetExceptions`, os dois tetos nulos = analisa tudo, `maxPets` que nunca tinha sido gravado e a janela de alta 15/12–15/03) — o padrão implícito do TypeScript é o que a regra 5 da modularização proíbe |
| 2026-09-04 | `pet_exception_phase2_automacao.sql` | **Política Pet, fase 2 — os textos do desfecho.** Cria os templates *Pet — exceção aprovada* e *Pet — exceção recusada* em PT/EN/ES e as duas regras de automação **DESLIGADAS** (`active=false`). Em 02/09 a decisão era não avisar o hóspede automaticamente; em 03/09 mudou — o AURA manda a mensagem e a pousada reforça e explica à mão depois. Nascem desligadas de propósito: recusar pet é conversa delicada e o texto precisa passar pela direção antes de chegar a um hóspede. Cuidado de esquema: `message_templates` **não tem a coluna `variables`**, apesar de `MessageTemplate` em `types/aura.ts` declarar `variables: string[]` — o tipo mente desde sempre, não inventar a coluna |
| 2026-09-04 | `modules_backfill_flags.sql` | **Modularização, fatia 1 — backfill explícito das flags de módulo** (`docs/MODULARIZATION.md` §6, regra 6). Até aqui `src/lib/modules.ts` tinha default por chave e `hasStock` nascia LIGADO para quem nunca opinou — medido em 02/09: a Estância do Vale (0 estadias) tinha Compras & Estoque no menu. A partir desta fatia o default de TODA chave é desligado e quem já existe recebe o valor gravado. O estoque é decidido pelo DADO (`EXISTS stock_settings`), não pelo nome da propriedade; as outras quatro (`hasGuarita`, `hasHsystem`, `hasRH`, `hasTimeclock`) eram desligadas por default e passam a estar escritas. Só toca chave que não é booleano (`jsonb_typeof … IS DISTINCT FROM 'boolean'`), então valor explícito existente é respeitado e a migration é idempotente. DEV em 04/09: `UPDATE 1 / 1 / 2 / 0 / 2` — a Estância perdeu o estoque que nunca teve; a Fazenda não mudou. Pode rodar antes ou depois do deploy — com a flag gravada, código velho e novo leem a mesma coisa |
| 2026-09-04 | `portal_gate_code.sql` | **Portal, senha do portão por propriedade.** A senha era o literal `"1008#"` fixo em `src/app/check-in/[code]/_portal/sheets.tsx` — TODA propriedade nova exibia a senha do portão da Fazenda aos hóspedes dela. Agora vira `settings.gateCode` (allowlist em `src/lib/property-settings.ts`, exposto em `/api/guest/session`); o `AccessSheet` lê de lá e mostra "não configurada" quando vazio. Backfill **explícito** de `"1008#"` só em `fazenda-do-rosa` (merge raso `|| jsonb_build_object`) — as demais ficam sem a chave de propósito, e o portal degrada limpo. Aplicado em PROD 04/09 (`UPDATE 1`, 45→46 chaves de settings). Reversível removendo a chave. Roda antes ou junto do deploy que leva o código novo, senão a Fazenda mostra "não configurada" na janela entre migration e deploy |
| 2026-09-05 | `structures_out_of_service_and_release_alert.sql` | **Área fechada: o alerta e a saída de operação sem unidades.** Duas colunas em `structures`. (1) `"outOfService"` (jsonb, mesmo shape de `"unitStatus"`) — estrutura INTEIRA fora de operação. Até aqui só a UNIDADE tinha estado persistente (`add_unit_status.sql`, 30/08), e quem não tem unidades cadastradas — em produção, 5 das 6 estruturas agendáveis — só podia sumir do portal relançando `maintenance_block` todo dia, exatamente a rotina que aquela migration matou para a jacuzzi. **Não confundir com a coluna legada `status`** (`available`/`maintenance`/…), que está no tipo e no modal de edição mas **nenhum código lê**: enum sem motivo, data nem autor não serve para o que a operação mostra ao hóspede, e ela segue morta de propósito. (2) `"releaseAlertSentFor"` (text, YYYY-MM-DD) — trava de repetição do push do cron `structure-release`, que roda a cada 15 min e sem ela avisaria de novo a cada rodada. Motivo do alerta, medido em produção: em 92 dias (06/06→05/09), **43 tiveram hóspede na casa e a jacuzzi nunca foi liberada**, nenhum deles por manutenção — e as 14 reservas de hóspede do período caíram TODAS em dia liberado, ou seja, nos 43 esquecidos ninguém pediu porque a área não existia no portal. Ambas nullable e sem default (ausência = em operação / nunca avisado); **não há backfill** — nenhuma linha existente muda de comportamento. Aplicar **antes do deploy**: o sino, o cron e `/api/guest/structure-bookings` passam a pedir `outOfService` no `select`, e sem a coluna o PostgREST erra e a leitura vira `null` — o mesmo modo de falha silenciosa que `add_unit_status.sql` documentou |
